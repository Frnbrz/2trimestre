import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'actividad.di',
  appName: 'actividad-di',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
